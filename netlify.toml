[build]
  command = "npm run build"
  publish = ".next"
  functions = ".netlify/functions/"

[build.environment]
  NEXT_PUBLIC_ANALYTICS_ID = ""
  NODE_VERSION = "20"
  NPM_FLAGS = "--legacy-peer-deps"

# Headers pour optimiser le cache et permettre la lecture vidéo
[[headers]]
  for = "/videos/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
    Access-Control-Allow-Origin = "*"

# Redirections pour SPA
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Configuration pour Next.js
[context.production.environment]
  NODE_VERSION = "20"

# Plugin Next.js pour Netlify
[[plugins]]
  package = "@netlify/plugin-nextjs"

# Cache plugin pour accélérer les builds
# Note: Plugin temporairement désactivé car non disponible sur npm
# [[plugins]]
#   package = "@netlify/plugin-cache"
#   [plugins.inputs]
#     paths = ["node_modules/.cache", ".next/cache"]